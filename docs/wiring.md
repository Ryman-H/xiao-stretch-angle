# Wiring

Default divider:

3.3V → R_FIXED (330kΩ) → A0 → stretch sensor → GND

Notes:
- If you invert resistor/sensor positions, the conversion formula changes.
- Keep wiring short/stable; noisy wiring will show up in Serial Plotter.
